name: PoC CI

on:
  pull_request:
    branches: [main]
    paths:
      - 'poc/**'
  push:
    branches: [main]
    paths:
      - 'poc/**'

defaults:
  run:
    working-directory: poc

jobs:
  lint-and-type-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          version: "latest"

      - name: Set up Python
        run: uv python install 3.14

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Run pre-commit hooks
        run: uv run pre-commit run --all-files

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          version: "latest"

      - name: Set up Python
        run: uv python install 3.14

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Download data
        run: uv run --package mcp-server python apps/mcp_server/src/tools/download_data.py

      - name: Transform data
        run: uv run --package mcp-server python apps/mcp_server/src/tools/load_jahresbericht.py

      - name: Run tests
        run: uv run pytest -v --tb=short
